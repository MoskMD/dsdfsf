#include <cstdio>

int main() {
    puts("Size of int: " + sizeof(int));
    puts("Size of short: " + sizeof(short));
    puts("Size of long: " + sizeof(long));
    puts("Size of char: " + sizeof(char));
    puts("Size of float: " + sizeof(float));
    puts("Size of double: " + sizeof(double));

    int intSize = sizeof(int);
    int shortSize = sizeof(short);
    int longSize = sizeof(long);
    int charSize = sizeof(char);
    int floatSize = sizeof(float);
    int doubleSize = sizeof(double);

    return 0;
}
